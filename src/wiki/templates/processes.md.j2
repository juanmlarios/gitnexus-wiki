# Execution flows

{{ processes | length }} process(es) traced by gitnexus across `{{ repo }}`. Each row is a chain of function calls reachable from a single entry point. Click into [clusters/](clusters/) for the surrounding code.

{% if by_cluster %}
{% for cluster, procs in by_cluster.items() %}
## {{ cluster }}

| Process | Steps | Chain |
|---------|-------|-------|
{% for p in procs %}
| {{ p.label }} | {{ p.step_count }} | {{ p.chain | join(' → ') }} |
{% endfor %}

{% endfor %}
{% else %}
## All flows

| Process | Steps | Chain |
|---------|-------|-------|
{% for p in processes %}
| {{ p.label }} | {{ p.step_count }} | {{ p.chain | join(' → ') }} |
{% endfor %}
{% endif %}

---

> Generated by [gitnexus-wiki](https://github.com/juanmlarios/gitnexus-wiki). Every step name is a real symbol in the gitnexus index.
